FROM gradle:9.2.1-jdk25-ubi10 AS builder

WORKDIR /workspace
COPY backend/ ./
RUN gradle :agent:bootJar -x test --no-daemon

FROM eclipse-temurin:25-jre
WORKDIR /app
COPY --from=builder /workspace/agent/build/libs/agent-*.jar /app/app.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]
